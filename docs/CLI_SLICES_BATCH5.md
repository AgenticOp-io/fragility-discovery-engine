# CLI smoke slices — batch 5 (ten slices)

Charter docs, bundled attribution pins, and Phase N / composite / eval-pool parity after batch 4. See [`CLI_SLICES.md`](CLI_SLICES.md).

| # | Slice | What it guards |
|---|--------|----------------|
| 1 | `BOUNDARIES.md` Phase L **v4/penta** + Phase N five-domain composite exit | Charter matches shipped composite + LLM packs |
| 2 | `test_bundled_liquidity_ladder_chain_path_trace` | Bundled liquidity mutation-chain path trace |
| 3 | `test_bundled_liquidity_ladder_merge_attribution_schema` | Bundled liquidity joint merge JSON |
| 4 | `narrate_frozen_json` on bundled **aggregate** replay | Narration on `sample_replay.json` |
| 5 | Penta composite `narrate` + `--cite-digest` | Citation block on v4 composite |
| 6 | `benchmark_rollout` liquidity GA `--eval-pool processes` | Picklable bundle search on Phase N frozen bundle |
| 7 | `reviewer_memo_v1` LLM pack on bundled penta | Reviewer prompt on v4 composite |
| 8 | `plot_pareto_front` on bundled liquidity Pareto | Figure hook on Phase N archive |
| 9 | Liquidity GA `--eval-workers 2` + `benchmark_rollout` MC on `liquidity_ladder_rollout_v1` | Search + frozen bundle microbench |
| 10 | `plot_replay_timeline` liquidity bundled + ε-sweep liquidity `--continue-after-collapse` (+ `meta.continue_after_collapse`) + `institution_composite_v1` on quad | Replay viz, recovery sweep meta, legacy composite pack |
