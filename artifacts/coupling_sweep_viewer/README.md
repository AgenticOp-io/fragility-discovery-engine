# Coupling sweep viewer

Static chart for `coupled-institution-coupling-sweep-v1` JSON from the research fork.

Open via HTTP from repo root: `artifacts/coupling_sweep_viewer/index.html`

Public demo: `/artifacts/coupling_sweep_viewer/index.html` (auto-loads bundled sweep).
