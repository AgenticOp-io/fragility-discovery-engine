[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fragility-engine"
version = "0.5.0"
description = "Deterministic search and stress-style exploration over modular simulations, with JSON replay and benchmark checks."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = ["numpy>=1.26", "networkx>=3.2"]
keywords = ["simulation", "robustness", "stress-testing", "counterfactual", "benchmark"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: Implementation :: CPython",
    "Topic :: Scientific/Engineering",
]

[project.urls]
Homepage = "https://github.com/AgenticOp-io/fragility-discovery-engine"
Repository = "https://github.com/AgenticOp-io/fragility-discovery-engine"
Documentation = "https://github.com/AgenticOp-io/fragility-discovery-engine/blob/main/docs/WHITEPAPER_INTRODUCTION.md"
Issues = "https://github.com/AgenticOp-io/fragility-discovery-engine/issues"

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.8.0", "matplotlib>=3.8.0"]
viz = ["matplotlib>=3.8.0"]
accelerate = ["numba>=0.59"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]

[tool.ruff]
target-version = "py311"
line-length = 120
src = ["src", "tests", "scripts"]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

